# Just make changes to your files, add them and commit them using Git, and the push them.

# Before uploading/pushing changes, test them locally (127.0.0.1)

# From the directory where the app exists, run: $ heroku login -> and login to your Heroku account.

# heroku info -> to make sure we are on the appropiate heroku app. (Otherwise, change it using: heroku git:remote --app <app-name>)

# git add .

# git commit -m "Added something"

# git push heroku master

# Open: https://<app-name>herokuapp.com -> Changes should be reflected.
